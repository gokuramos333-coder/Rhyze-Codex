[build]
  command = "npm run netlify:stage-native-bindings && npx prisma generate && NEXT_DIST_DIR=.next next build"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"
  STORAGE_DRIVER = "netlify"
  NETLIFY_NEXT_SKEW_PROTECTION = "true"
